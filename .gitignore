node_modules/
.next/
out/
dist/
build/
coverage/
apps/render-worker/renders/
# Voice model downloaded at runtime (~78MB) - never belongs in the repo.
apps/render-worker/.voice-cache/

# Graphify output. graph.html and graph.json are ~1MB of regenerable artifacts
# that churn completely on every run; the report is small, readable and worth
# tracking, so it is the one exception.
graphify-out/*
!graphify-out/GRAPH_REPORT.md

.env
.env.*
!.env.example
!.env.local.example

*.log
.DS_Store
Thumbs.db
.vercel
.supabase/
supabase/.temp/
